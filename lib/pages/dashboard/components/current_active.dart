import 'package:flutter/material.dart';
import 'package:kstmis/styles/constants.dart';

class CurrentActive extends StatelessWidget {
  const CurrentActive({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
